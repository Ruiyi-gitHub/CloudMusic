<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper为映射的根节点，namespace指定Dao接口的完整类名
mybatis会依据这个接口动态创建一个实现类去实现这个接口，
而这个实现类是一个Mapper对象

   id ="接口中的方法名"
　　parameterType="传入的参数类型"
　　resultType = "返回实体类对象，使用包.类名"-->
<mapper namespace="com.airmusic.dao.UserMapper">
    <insert id="importPicturePath" parameterType="com.airmusic.entiy.Picture">
        insert into a_picture(pictureTitle,picturePath) values
        <foreach collection="collection,list" item="PathName" separator=",">
            (#{PathName.pictureTitle},#{PathName.picturePath})
        </foreach>
    </insert>

    <insert id="addSongListger" parameterType="com.airmusic.entiy.SongList">
        insert into
        a_songset(mName,mCreateName)
        values
        (#{mName},#{mCreateName})
    </insert>
</mapper>
